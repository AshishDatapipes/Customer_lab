version: 2

sources:
  - name: ga4
    schema: raw_ga4 # replace with your actual raw dataset name in BigQuery
    tables:
      - name: events
        loaded_at_field: event_timestamp
        freshness:
          warn_after: { count: 15, period: minute }
          error_after: { count: 60, period: minute }
